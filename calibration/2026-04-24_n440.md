# Calibration Report — 2026-04-24, 1305 jobs

**Batch:** `2026-04-24_n440` (covers Stage 1 + Stage 2 runs)
**Threshold change applied this pass:** `semantic_filter_threshold` 0.30 → 0.27
**Profile pack change proposed:** None — no changes recommended yet.
**Risk level:** Low

---

## Executive summary

- 1,305 jobs processed from the NAV feed (active, future deadline, Oslo/Akershus/Vestfold/Agder corridor).
- 99.5% SKIP — expected on a general Norwegian labour market feed.
- 1 APPLY, 5 REVIEW_LOW. All 6 KEEPs are plausible.
- Primary skip cause: geo filter (673 jobs, 52%) — the NAV feed is national, most jobs are rural/wrong region.
- Secondary: semantic below threshold (199 jobs, 15%) — threshold lowered from 0.30 → 0.27 after Stage 1 showed borderline relevant roles scoring 0.27–0.29.
- Hard NO title regex caught 106 jobs (8%) — working correctly (nurse, butikk, håndverk, etc).
- 327 "other" skips: LLM triage decided SKIP on jobs that passed geo + semantic + hard_no. That's the nano model doing its job.
- **No profile_pack.md changes proposed.** Reconfirm after Stage 3 (500 jobs) with more signal.

---

## Proposed profile_pack.md diff

**None for this pass.** The calibration issue is threshold and NAV feed composition, not profile_pack content. The two target roles sections (1: target titles, 4: hard NOs) appear to be working as intended given the KEEPs.

---

## KEPT jobs (6)

| Decision | Title | Employer | Note |
|---|---|---|---|
| **APPLY** | Senior Product Manager - Data & AI focus | Nofence | ✅ Exactly right — product + AI focus, senior |
| REVIEW_LOW | Senior Mulesoft Developer | reMarkable | ⚠️ Technical role — integration/platform angle plausible but core is dev |
| REVIEW_LOW | Senior Software Engineer (Rust) | Six Robotics | ⚠️ Engineering — probably too technical, but Six Robotics interesting company |
| REVIEW_LOW | Senior Platform Engineer | Six Robotics | ⚠️ Platform angle — closer to scope than pure dev |
| REVIEW_LOW | Platform Reliability Engineer | Heimdall Power | ⚠️ Platform/reliability — borderline, good company |
| REVIEW_LOW | Gi kraft til luft- og kystforsvar — elektrisk hardware | Kongsberg Defence | ❌ Hardware engineering — false positive, wrong domain |

**Lars overrides needed:** Add your decisions to `calibration/overrides.jsonl` (see format below).

---

## Top false positives (from sample)

1. **Kongsberg Defence — hardware engineer** — should be SKIP. Title contains "elektrisk hardware" — not a product/digital role. The semantic model is seeing "forsvar/forsvarssystemer" as adjacent to governance/management vocabulary. Possible fix: add `elektrisk hardware|hardwareingeniør` to `hard_no_title_regex` — but hold until Stage 3 confirms pattern.

---

## Top false negatives observed

None confirmed yet — this requires Lars override data from `overrides.jsonl`. The semantic threshold change (→0.27) may surface more candidates in Stage 3. Check the boundary_scores list in the JSON for specific jobs at 0.27–0.30.

---

## Threshold observations

- 159 jobs scored in the 0.24–0.33 boundary band.
- Mean sim score across all scored jobs: see JSON `threshold_observations.mean_sim_score_all`.
- New threshold 0.27 will open ~30–50 more jobs per 100 for LLM triage (nano model, cheap).
- **Watch for:** Stage 3 REVIEW rate going up significantly. If REVIEW > 15%, threshold may be too low and should go back to 0.28–0.29.

---

## Geo observations

- 673 geo postal skips (52%) — NAV feed is national, this is correct behaviour.
- Passed postal prefixes: Oslo (0xxx) dominant, Akershus (1xxx) present, Vestfold/Telemark (3xxx) and Agder (4xxx) smaller.
- Geo filter is working correctly. No changes recommended.
- **Note for crew:** Agder corridor jobs (4xxx) that pass geo are commute-feasible from Arendal if Lars is considering that. The tracker tag `commute_agder` could be useful in Phase 5.

---

## Overrides format

Add to `calibration/overrides.jsonl` — one JSON object per line:

```json
{"job_id": "nav-xxx", "title": "...", "override": "SKIP", "note": "hardware, not product"}
{"job_id": "nav-yyy", "title": "...", "override": "APPLY", "note": "should have been kept"}
```

The crew's `calibrate` task reads this file. Overrides accumulate across passes.

---

## Recommendation

- **Accept** threshold change (0.30 → 0.27). Already applied to `configs/pipeline.v1.yaml`.
- **No profile_pack changes** — too early, too little APPLY signal.
- **Stage 3 (500 jobs):** watch REVIEW rate. If > 15%, nudge threshold back to 0.28. If APPLY rate stays near 0.1%, profile_pack section 1 (target titles) may need new alternations.
- **Add overrides** for the 6 KEEPs above before crew's `calibrate` task runs.

---

## Files

- JSON: `calibration/2026-04-24_n440.json`
- Raw JSONL: `calibration/2026-04-24_n440_raw.jsonl`
- Overrides: `calibration/overrides.jsonl` (bootstrap below)
